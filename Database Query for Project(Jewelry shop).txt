CREATE TABLE userss (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL
);

INSERT INTO userss (email, password) 
VALUES ('amresh1234@gmail.com', crypt('8624807723', gen_salt('bf')));


select * from userss;


CREATE EXTENSION IF NOT EXISTS pgcrypto;


CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL UNIQUE
);


INSERT INTO categories (name) VALUES ('Necklace'), ('Bracelet'), ('Earrings'), ('Ring');


CREATE TABLE jewelry (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    type VARCHAR(100) NOT NULL,
    quantity INTEGER NOT NULL CHECK (quantity >= 0),
    carat DECIMAL(5,2) NOT NULL CHECK (carat > 0),
    price DECIMAL(10,2) NOT NULL CHECK (price > 0),
    gst DECIMAL(5,2) NOT NULL CHECK (gst >= 0),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

select * from jewelry;

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    role VARCHAR(50) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


select * from users;



